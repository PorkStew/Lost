<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    //these will stay selected


        <group android:checkableBehavior="single">
            <item android:id="@+id/nav_profile"
                android:icon="@drawable/ic_account"
                android:title="Profile"/>
            <item android:id="@+id/nav_navigation"
                android:icon="@drawable/ic_navigation"
                android:title="Navigation"/>
            <item android:id="@+id/nav_preferences"
                android:icon="@drawable/ic_preferences"
                android:title="Preferences"/>
            <item android:id="@+id/nav_information"
                android:icon="@drawable/ic_info"
                android:title="About"/>
            <item   android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="logout"/>
    </group>
</menu>